version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: semina-mongodb
    ports:
      - '27018:27017'
    volumes:
      - semina-mongo-data:/data/db
    restart: unless-stopped

volumes:
  semina-mongo-data:
